# Environment variable names only — DO NOT commit secrets here
NODE_ENV=
PORT=
FRONTEND_ORIGIN=
RATE_LIMIT_MAX=
# Payment provider credentials (set in deployment only)
PAYMENT_PROVIDER=
PAYMENT_API_KEY=
PAYMENT_API_SECRET=
