const BLOCKED_KEYS = new Set(["__proto__","prototype","constructor"]);
export function assertPlainData(value,path="input",depth=0){if(depth>32)throw new Error(`${path} exceeds maximum nesting depth.`);if(value===null||["string","number","boolean"].includes(typeof value)){if(typeof value==="number"&&!Number.isFinite(value))throw new Error(`${path} contains a non-finite number.`);return true;}if(Array.isArray(value)){if(value.length>100000)throw new Error(`${path} exceeds maximum item count.`);value.forEach((v,i)=>assertPlainData(v,`${path}[${i}]`,depth+1));return true;}if(typeof value!=="object")throw new Error(`${path} contains unsupported data.`);for(const [k,v] of Object.entries(value)){if(BLOCKED_KEYS.has(k))throw new Error(`${path} contains a blocked key.`);assertPlainData(v,`${path}.${k}`,depth+1);}return true;}
export function requireAuthorizedContext(context){if(!context||context.authorized!==true||typeof context.principal!=="string"||!context.principal.trim())throw new Error("Authorized principal is required.");return true;}
